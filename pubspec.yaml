name: _imagineeringwithus_pack
description: Private widgets package.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
    
  intl: ">=0.17.0" 
  google_fonts: ">=3.0.1"
  flutter_staggered_animations: ">=1.1.1"
  flutter_svg: ">=2.0.0"
  lottie: ">=2.3.2"
  cached_network_image: ">=3.2.3"
  octo_image: ">=1.0.2"
  dash_flags: ^0.0.4
  logger: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ 
    - assets/images/png/
